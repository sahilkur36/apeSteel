# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtualenvs
.venv/
venv/
ENV/
env/

# Tooling caches
.pytest_cache/
pytest-cache-files-*/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# IDE / local agent config
.vscode/
.idea/
.claude/settings.local.json
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-specific: keep the AISC pickle in git, but ignore intermediate
# CSV dumps the catalog ingestion script produces.
*.pkl.tmp

# Copyrighted AISC PDF excerpts staged locally for the Phase F citation
# work — never commit (source PDFs live in the engineer's library).
docs/design_notes/_aisc_src_extract/

# Local-only experiments — anything under to_review/ stays out of the wheel
# but is tracked in git for reference.
